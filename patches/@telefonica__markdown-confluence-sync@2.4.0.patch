diff --git a/dist/lib/confluence/transformer/support/rehype/rehype-replace-img-tags.js b/dist/lib/confluence/transformer/support/rehype/rehype-replace-img-tags.js
index becdc594dc728755f1684ee329874627a92adad6..1f47f933e8562d5371768f1ed9493fd7063722b9 100644
--- a/dist/lib/confluence/transformer/support/rehype/rehype-replace-img-tags.js
+++ b/dist/lib/confluence/transformer/support/rehype/rehype-replace-img-tags.js
@@ -68,11 +68,11 @@ const rehypeReplaceImgTags = function rehypeReplaceImgTags() {
  * @returns object with svg properties
  */
 function obtainSvgProperties(src) {
-    const mermaidFilePattern = new RegExp("autogenerated.+.svg$", "g");
-    return mermaidFilePattern.test(src)
-        ? {
-            "ac:width": "1000",
-        }
-        : {};
+    const mermaidFilePattern = /autogenerated-.+\.svg$/;
+    if (!mermaidFilePattern.test(src)) {
+        return {};
+    }
+    const widthMatch = src.match(/-w(\d+)\.svg$/);
+    return widthMatch ? { "ac:width": widthMatch[1] } : {};
 }
 export default rehypeReplaceImgTags;
diff --git a/dist/lib/confluence/transformer/support/rehype/rehype-replace-internal-references.js b/dist/lib/confluence/transformer/support/rehype/rehype-replace-internal-references.js
index 964459b80bc6d153d9d71cca49e03a8e6e3cf8cb..503c0f48f910d464b57d8e0405fd47363b41d927 100644
--- a/dist/lib/confluence/transformer/support/rehype/rehype-replace-internal-references.js
+++ b/dist/lib/confluence/transformer/support/rehype/rehype-replace-internal-references.js
@@ -44,7 +44,10 @@ const rehypeConfluenceStorage = function rehypeConfluenceStorage({ spaceKey, pag
             if (!node.properties.href.startsWith(".")) {
                 return node;
             }
-            const referencedPagePath = resolve(dirname(file.path), node.properties.href);
+            // Patch: remark->rehype percent-encodes the href (spaces become %20), but the
+            // pages map is keyed by raw file paths, so an encoded href never matches and the
+            // link is dropped. Decode before resolving so filenames with spaces resolve.
+            const referencedPagePath = resolve(dirname(file.path), decodeURIComponent(node.properties.href));
             const referencedPage = pages.get(referencedPagePath);
             if (referencedPage === undefined) {
                 file.message("Internal reference to non-existing page", node.position);
diff --git a/dist/lib/confluence/transformer/support/remark/remark-replace-mermaid.js b/dist/lib/confluence/transformer/support/remark/remark-replace-mermaid.js
index a39c1f658080f807a9ba1922b5deb5d492d2ba68..1893057ca1204c978f45cafb6a6d65ded7297eac 100644
--- a/dist/lib/confluence/transformer/support/remark/remark-replace-mermaid.js
+++ b/dist/lib/confluence/transformer/support/remark/remark-replace-mermaid.js
@@ -25,7 +25,8 @@ const remarkReplaceMermaid = function remarkReplaceMermaid(options) {
 function replaceMermaidCodeBlocks(tree, file, outDir) {
     replace(tree, isMermaidCode, function (node) {
         try {
-            const url = render(outDir, node.value);
+            const width = parseMermaidWidth(node.meta);
+            const url = render(outDir, node.value, width);
             return {
                 type: "image",
                 url,
@@ -42,7 +43,18 @@ function replaceMermaidCodeBlocks(tree, file, outDir) {
 function isMermaidCode(node) {
     return node.type === "code" && node.lang === "mermaid";
 }
-function render(dir, code) {
+export function parseMermaidWidth(meta) {
+    if (typeof meta !== "string") {
+        return undefined;
+    }
+    const match = meta.match(/(?:^|\s)width=(\d+)(?:\s|$)/);
+    if (!match) {
+        return undefined;
+    }
+    const width = Number.parseInt(match[1], 10);
+    return Number.isInteger(width) && width > 0 ? width : undefined;
+}
+function render(dir, code, width) {
     const tempFileName = randomBytes(4).toString("hex");
     const mmdTempFileName = `${tempFileName}.mmd`;
     const mmdcTempFile = join(dir, mmdTempFileName);
@@ -52,7 +64,8 @@ function render(dir, code) {
         const mmdcExec = which.sync("mmdc", {
             path: DEPENDENCIES_BIN_PATH,
         });
-        const svgTempFilename = `${AUTOGENERATED_FILE_PREFIX}${tempFileName}.svg`;
+        const widthSuffix = width ? `-w${width}` : "";
+        const svgTempFilename = `${AUTOGENERATED_FILE_PREFIX}${tempFileName}${widthSuffix}.svg`;
         const svgTempFile = join(dir, svgTempFilename);
         writeFileSync(mmdcTempFile, code, { flag: "w+" });
         const puppeteerConfigFile = resolve(join(PACKAGE_ROOT, "config"), "puppeteer-config.json");
